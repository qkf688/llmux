# 模型同步日志页面增强计划

## 需求概述

在"模型同步日志"页面增加一个统计信息卡片，显示：
- 最近同步时间
- 下一次自动同步时间
- 总提供商数量
- 有更新的提供商数量
- 没变化的数量
- 返回空模型的数量
- 报错的数量
- "立即同步"按钮

## 实现步骤

### 1. 后端 API 新增

**新增接口：** `GET /model-sync/stats`

**返回数据结构：**
```typescript
interface ModelSyncStats {
  last_sync_at: string | null;          // 最近同步时间
  next_sync_at: string | null;          // 下次自动同步时间
  sync_enabled: boolean;                // 是否启用自动同步
  sync_interval: number;                // 同步间隔(小时)
  total_providers: number;              // 总提供商数量
  providers_with_updates: number;       // 有更新的提供商数量
  providers_no_changes: number;         // 没变化的数量
  providers_empty_models: number;       // 返回空模型的数量
  providers_with_errors: number;        // 报错的数量
}
```

### 2. 后端 Handler 实现

**文件：** `handler/api.go`

新增函数：
- `GetModelSyncStats(c *gin.Context)` - 获取同步统计信息

**统计逻辑：**
1. 查询最近一次同步日志获取 `last_sync_at`
2. 根据同步间隔计算 `next_sync_at`
3. 查询所有启用模型端点的提供商总数
4. 从最近一次同步结果中统计各类别数量

### 3. 前端 API 函数

**文件：** `webui/src/lib/api.ts`

新增函数：
```typescript
export async function getModelSyncStats(): Promise<ModelSyncStats>
```

### 4. 前端页面增强

**文件：** `webui/src/routes/model-sync-logs.tsx`

修改内容：
1. 引入 Card 组件和统计相关组件
2. 新增 `stats` 状态存储统计数据
3. 新增 `fetchStats()` 函数获取统计数据
4. 在页面顶部添加统计信息卡片
5. 添加"立即同步"按钮及处理逻辑
6. 立即同步后刷新统计数据和日志列表

**UI 设计：**
```
┌─────────────────────────────────────────────────────────────┐
│  📊 模型同步状态                                    [立即同步] │
├─────────────────────────────────────────────────────────────┤
│  上次同步: 2024-01-15 14:30:00      下次同步: 2024-01-16 02:30:00 │
│                                                             │
│  总提供商: 5    有更新: 2    无变化: 2    空模型: 0    报错: 1 │
└─────────────────────────────────────────────────────────────┘
```

### 5. 文件修改清单

| 文件 | 操作 |
|------|------|
| `handler/api.go` | 新增 `GetModelSyncStats` 函数 + 路由注册 |
| `main.go` | 新增路由 `GET /model-sync/stats` |
| `webui/src/lib/api.ts` | 新增 `getModelSyncStats` 函数和类型定义 |
| `webui/src/routes/model-sync-logs.tsx` | 新增统计卡片和立即同步功能 |

## 数据流

```
用户访问页面
    │
    ▼
┌──────────────┐
│ 前端获取统计  │◄──── GET /model-sync/stats
└──────┬───────┘
       │
       ▼
┌──────────────┐
│ 后端查询数据库 │
└──────┬───────┘
       │
       ▼
返回统计信息 JSON
    │
    ▼
前端渲染统计卡片 + 日志列表
```

## 注意事项

1. 同步操作可能需要较长时间，建议异步处理
2. "立即同步"按钮点击后显示 loading 状态
3. 同步完成后自动刷新统计数据
4. 错误信息需要友好展示
